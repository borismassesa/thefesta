import { type ReactNode } from 'react'
import OpusPassDigitalCardsCmsLayout from '../digital-cards/layout'

export default function TicketCmsLayout({ children }: { children: ReactNode }) {
  return <OpusPassDigitalCardsCmsLayout>{children}</OpusPassDigitalCardsCmsLayout>
}
